# Test generated by RoostGPT for test gofinance-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:

  - name: "Successful API request"
    flow:
      - get:
          url: "/account/graph/123/TypeA"
          expect:
            - statusCode: 200
            - hasProperty: "countGraph"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/account/graph/123/"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/account/graph/1234567890/TypeVeryLongAndSpecialCharacters"
          expect:
            - statusCode: "{{ 200..500 }}"
            - hasProperty: "countGraph"
