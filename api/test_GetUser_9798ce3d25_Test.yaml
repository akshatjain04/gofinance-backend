# Test generated by RoostGPT for test gofinance-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/user/johndoe"
          expect:
            - statusCode: 200
            - hasProperty: "username"
            - equals:
                - "username"
                - "johndoe"
  
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/user/"
          expect:
            - statusCode: 400

  - name: "API request with non-existing user"
    flow:
      - get:
          url: "/user/nonexistentuser"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/user/johndoe!@#$%^&*()"
          expect:
            - statusCode: 400
