# Test generated by RoostGPT for test gofinance-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: 'http://your-api-url.com'
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    id:
      - '1'
      - '2'
      - '3'
scenarios:
  - name: 'Successful API request'
    flow:
      - post:
          url: '/account/{{ id }}'
          json:
            Title: 'Test Title'
            Description: 'Test Description'
            Value: 'Test Value'
          headers:
            Authorization: 'Bearer valid_token'
          expect:
            - statusCode: 200
            - hasProperty: 'Title'
            - hasProperty: 'Description'
            - hasProperty: 'Value'

  - name: 'API request with validation failure'
    flow:
      - post:
          url: '/account/{{ id }}'
          json:
            Title: 'Test Title'
            Description: 'Test Description'
          headers:
            Authorization: 'Bearer valid_token'
          expect:
            - statusCode: 400
            - hasProperty: 'error'

  - name: 'API request with edge case'
    flow:
      - post:
          url: '/account/{{ id }}'
          json:
            Title: 'Test Title with special characters !@#$%^&*()'
            Description: 'Test Description with large data' + 'a'.repeat(10000)
            Value: 'Test Value'
          headers:
            Authorization: 'Bearer valid_token'
          expect:
            - statusCode: 200
            - hasProperty: 'Title'
            - hasProperty: 'Description'
            - hasProperty: 'Value'
