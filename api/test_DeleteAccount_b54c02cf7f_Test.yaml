# Test generated by RoostGPT for test gofinance-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "data.csv"
    fields:
      - "id"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/account/{{ id }}"
          headers:
            Authorization: "Bearer valid_token"
          expect:
            - statusCode: 200
            - hasProperty: "success"
            - equals:
                success: true
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/account/invalid_id"
          headers:
            Authorization: "Bearer valid_token"
          expect:
            - statusCode: 400
            - hasProperty: "error"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/account/{{ id }}"
          headers:
            Authorization: "Bearer valid_token_with_special_characters"
          expect:
            - statusCode: "{{ statusCode }}"
            - hasProperty: "success"
